# Meme Generator

A simple React application that allows users to generate memes by adding custom top and bottom text to random images. Features include:

- Fetches memes from the [Imgflip API](https://api.imgflip.com/get_memes)
- Custom top and bottom text
- Dark mode toggle
- Responsive design for mobile and desktop
